import { initFederationOverrides } from './init-federation-overrides';

describe('initFederationOverrides', () => {
  it('should work', () => {
    expect(initFederationOverrides()).toEqual('init-federation-overrides');
  });
});
